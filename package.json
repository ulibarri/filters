{
  "name": "csv-diff-app-v2",
  "version": "2.0.0",
  "description": "V2: Upload Draft (xlsx/xls/csv) + Specialty (csv). The app generates Filters.csv from Draft (removing Glenner/Type-list/dialysis rows) and compares it against Specialty by CSN. Independent project from V1 (production).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "csv-parse": "^5.6.0",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
